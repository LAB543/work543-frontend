import React from 'react';
import Nav from './Nav';
import Sidebar from './Sidebar';

const Header = () => {
  return (
    <div className="header">
      <Nav />
    </div>
  );
};

export default Header;
